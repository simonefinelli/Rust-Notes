#[derive(PartialEq, Debug)]  // this line of code is related with 'trait' topic (we learn about it in next sections)

pub enum Category {  // a public element of a sub-module can be only accessed by parent module
Electronics,
    Clothing,
    Books
}