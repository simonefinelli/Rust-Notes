/*
    Here we can view how to organizing Libraries (and their modules)
    to better organize the tree structure.

    In Rust there are two methods to organize files.

    Here we show the second approach: use explicit folders to contain the modules.
 */

fn main() {
    println!("Hello, world!");
}
